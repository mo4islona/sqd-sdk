{
    "name": "@sqd-sdk/core",
    "version": "0.0.0",
    "license": "GPL-3.0-or-later",
    "publishConfig": {
        "access": "public"
    },
    "files": ["lib"],
    "main": "./lib/index.cjs",
    "module": "./lib/index.js",
    "types": "./lib/index.d.ts",
    "scripts": {
        "build": "tsup",
        "build:dev": "tsup --config tsup.config.dev.ts",
        "pack": "(cd lib && npm pack --pack-destination ..) && rm -f package.tgz && mv sqd-sdk-core-*.tgz package.tgz",
        "prepublishOnly": "pnpm build && pnpm pack",
        "publish": "npm publish package.tgz",
        "postpublish": "rm -f package.tgz"
    },
    "dependencies": {
        "supports-color": "^8.1.1"
    },
    "devDependencies": {
        "@types/node": "^18.19.83",
        "@types/supports-color": "^8.1.3",
        "esbuild-fix-imports-plugin": "^1.0.19",
        "esbuild-plugin-alias": "^0.2.1",
        "esbuild-plugin-tsconfig-paths": "^1.0.0",
        "typescript": "~5.5.4"
    }
}
